{% extends 'base.html.twig' %}

{% block title %}RoomEquipment{% endblock %}

{% block body %}
    <h1>RoomEquipment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ room_equipment.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ room_equipment.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_room_equipment_index') }}">back to list</a>

    <a href="{{ path('app_admin_room_equipment_edit', {'id': room_equipment.id}) }}">edit</a>

    {{ include('admin_room_equipment/_delete_form.html.twig') }}
{% endblock %}
