<form method="post" action="{{ path('app_admin_physician_delete', {'id': physician.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ physician.id) }}">
    <button class="btn">Delete</button>
</form>
