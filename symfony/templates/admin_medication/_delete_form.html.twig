<form method="post" action="{{ path('app_admin_medication_delete', {'id': medication.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medication.id) }}">
    <button class="btn">Delete</button>
</form>
