{% extends 'admin/base_admin.html.twig' %}

{% block title %}Medication{% endblock %}

{% block body %}
    <h1>Medication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medication.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ medication.name }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ medication.brand }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ medication.price }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ medication.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_medication_index') }}">back to list</a>

    <a href="{{ path('app_admin_medication_edit', {'id': medication.id}) }}">edit</a>

    {{ include('admin_medication/_delete_form.html.twig') }}
{% endblock %}
