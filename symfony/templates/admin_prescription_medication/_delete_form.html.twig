<form method="post" action="{{ path('app_admin_prescription_medication_delete', {'id': prescription_medication.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prescription_medication.id) }}">
    <button class="btn">Delete</button>
</form>
