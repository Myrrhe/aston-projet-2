{% extends 'admin/base_admin.html.twig' %}

{% block title %}PrescriptionMedication{% endblock %}

{% block body %}
    <h1>PrescriptionMedication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prescription_medication.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ prescription_medication.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_prescription_medication_index') }}">back to list</a>

    <a href="{{ path('app_admin_prescription_medication_edit', {'id': prescription_medication.id}) }}">edit</a>

    {{ include('admin_prescription_medication/_delete_form.html.twig') }}
{% endblock %}
