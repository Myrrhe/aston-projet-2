<form method="post" action="{{ path('app_admin_physician_specialization_delete', {'id': physician_specialization.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ physician_specialization.id) }}">
    <button class="btn">Delete</button>
</form>
