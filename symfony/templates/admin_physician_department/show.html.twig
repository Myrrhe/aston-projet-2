{% extends 'base.html.twig' %}

{% block title %}PhysicianDepartment{% endblock %}

{% block body %}
    <h1>PhysicianDepartment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ physician_department.id }}</td>
            </tr>
            <tr>
                <th>PrimaryAffiliation</th>
                <td>{{ physician_department.primaryAffiliation ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_physician_department_index') }}">back to list</a>

    <a href="{{ path('app_admin_physician_department_edit', {'id': physician_department.id}) }}">edit</a>

    {{ include('admin_physician_department/_delete_form.html.twig') }}
{% endblock %}
