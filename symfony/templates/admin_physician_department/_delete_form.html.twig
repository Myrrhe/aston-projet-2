<form method="post" action="{{ path('app_admin_physician_department_delete', {'id': physician_department.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ physician_department.id) }}">
    <button class="btn">Delete</button>
</form>
