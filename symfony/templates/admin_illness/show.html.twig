{% extends 'base.html.twig' %}

{% block title %}Illness{% endblock %}

{% block body %}
    <h1>Illness</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ illness.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ illness.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_illness_index') }}">back to list</a>

    <a href="{{ path('app_admin_illness_edit', {'id': illness.id}) }}">edit</a>

    {{ include('admin_illness/_delete_form.html.twig') }}
{% endblock %}
