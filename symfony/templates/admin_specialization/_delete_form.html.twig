<form method="post" action="{{ path('app_admin_specialization_delete', {'id': specialization.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ specialization.id) }}">
    <button class="btn">Delete</button>
</form>
