{% extends 'admin/base_admin.html.twig' %}

{% block title %}Specialization index{% endblock %}

{% block body %}
    <h1>Specialization index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for specialization in specializations %}
            <tr>
                <td>{{ specialization.id }}</td>
                <td>{{ specialization.name }}</td>
                <td>
                    <a href="{{ path('app_admin_specialization_show', {'id': specialization.id}) }}">show</a>
                    <a href="{{ path('app_admin_specialization_edit', {'id': specialization.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_specialization_new') }}">Create new</a>
{% endblock %}
